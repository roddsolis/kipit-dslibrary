# Foundations

Los **foundations** de un **Design System** son el conjunto de principios y estilos básicos que establecen la identidad visual y la coherencia de una interfaz. Son la base sobre la que se construyen los componentes y aseguran una experiencia de usuario consistente.

```jsx
import { Button } from "@/components/ui/button";

export default function Example() {
  return <Button>Click Me</Button>;
}
```
